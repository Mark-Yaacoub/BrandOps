import { NextRequest, NextResponse } from "next/server";

// Temporary AI responses - يمكنك ربطه بـ OpenAI API لاحقاً
const responses: { [key: string]: string } = {
  default: "شكراً على سؤالك! هذه نسخة تجريبية من المساعد الذكي. قريباً سأتمكن من مساعدتك بشكل أفضل.",
  
  // Products related
  منتج: "يمكنني مساعدتك في إدارة المنتجات. يمكنك:\n• عرض جميع المنتجات من قائمة المنتجات\n• إضافة منتج جديد\n• تعديل تفاصيل المنتج\n• إضافة مكونات للمنتج",
  منتجات: "يمكنني مساعدتك في إدارة المنتجات. يمكنك:\n• عرض جميع المنتجات من قائمة المنتجات\n• إضافة منتج جديد\n• تعديل تفاصيل المنتج\n• إضافة مكونات للمنتج",
  
  // Tasks related
  مهمة: "لإدارة المهام، يمكنك:\n• عرض المهام من صفحة Tasks\n• إنشاء مهمة جديدة\n• تعيين المهمة لمستخدم\n• تغيير حالة المهمة (Pending, In Progress, Completed)",
  مهام: "لإدارة المهام، يمكنك:\n• عرض المهام من صفحة Tasks\n• إنشاء مهمة جديدة\n• تعيين المهمة لمستخدم\n• تغيير حالة المهمة (Pending, In Progress, Completed)",
  task: "لإدارة المهام، يمكنك:\n• عرض المهام من صفحة Tasks\n• إنشاء مهمة جديدة\n• تعيين المهمة لمستخدم\n• تغيير حالة المهمة (Pending, In Progress, Completed)",
  
  // Users related
  مستخدم: "لإدارة المستخدمين:\n• يمكن للـ Admin إضافة مستخدمين جدد من صفحة Users\n• تغيير كلمات المرور\n• حذف المستخدمين\n• تعيين الصلاحيات (Admin, Manager, User)",
  مستخدمين: "لإدارة المستخدمين:\n• يمكن للـ Admin إضافة مستخدمين جدد من صفحة Users\n• تغيير كلمات المرور\n• حذف المستخدمين\n• تعيين الصلاحيات (Admin, Manager, User)",
  
  // Batches related
  دفعة: "لإدارة الدفعات:\n• عرض جميع الدفعات من صفحة Batches\n• إنشاء دفعة جديدة\n• ربط الدفعة بالمنتجات\n• تتبع حالة الدفعة",
  دفعات: "لإدارة الدفعات:\n• عرض جميع الدفعات من صفحة Batches\n• إنشاء دفعة جديدة\n• ربط الدفعة بالمنتجات\n• تتبع حالة الدفعة",
  batch: "لإدارة الدفعات:\n• عرض جميع الدفعات من صفحة Batches\n• إنشاء دفعة جديدة\n• ربط الدفعة بالمنتجات\n• تتبع حالة الدفعة",
  
  // Help
  مساعدة: "أنا هنا لمساعدتك! يمكنني الإجابة عن:\n• كيفية استخدام المنتجات\n• إدارة المهام\n• إضافة مستخدمين\n• إنشاء الدفعات\n\nاسألني عن أي شيء!",
  help: "أنا هنا لمساعدتك! يمكنني الإجابة عن:\n• كيفية استخدام المنتجات\n• إدارة المهام\n• إضافة مستخدمين\n• إنشاء الدفعات\n\nاسألني عن أي شيء!",
  
  // Greetings
  مرحبا: "مرحباً بك! 👋 كيف يمكنني مساعدتك اليوم؟",
  مرحبً: "مرحباً بك! 👋 كيف يمكنني مساعدتك اليوم؟",
  السلام: "وعليكم السلام ورحمة الله وبركاته! 🌟 كيف يمكنني خدمتك؟",
  hello: "Hello! 👋 How can I help you today?",
  hi: "Hi there! 👋 What can I do for you?",
};

function findResponse(message: string): string {
  const lowerMessage = message.toLowerCase();
  
  for (const [keyword, response] of Object.entries(responses)) {
    if (lowerMessage.includes(keyword.toLowerCase())) {
      return response;
    }
  }
  
  return responses.default;
}

export async function POST(request: NextRequest) {
  try {
    const { message } = await request.json();

    if (!message) {
      return NextResponse.json(
        { success: false, error: "Message is required" },
        { status: 400 }
      );
    }

    // Simulate AI thinking delay
    await new Promise((resolve) => setTimeout(resolve, 500));

    const response = findResponse(message);

    return NextResponse.json({
      success: true,
      response,
    });
  } catch (error) {
    console.error("Error in AI chat:", error);
    return NextResponse.json(
      { success: false, error: "Failed to process message" },
      { status: 500 }
    );
  }
}
